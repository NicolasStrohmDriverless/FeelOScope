<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FeelOScope" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brand_gradient_start</item>
        <item name="colorPrimaryVariant">@color/brand_gradient_end</item>
        <item name="colorOnPrimary">@color/text_primary_on_dark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/accent_blue</item>
        <item name="colorSecondaryVariant">@color/accent_lilac</item>
        <item name="colorOnSecondary">@color/text_primary_on_dark</item>
        <item name="android:colorBackground">@color/brand_background_dark</item>
        <item name="android:navigationBarColor">@color/brand_background_dark</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Customize your theme here. -->
    </style>
</resources>
