<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:bottomLeftRadius="28dp" android:bottomRightRadius="28dp" />
    <gradient
        android:angle="0"
        android:startColor="@color/toolbar_background_start"
        android:centerColor="@color/toolbar_background_center"
        android:endColor="@color/toolbar_background_end" />
</shape>
