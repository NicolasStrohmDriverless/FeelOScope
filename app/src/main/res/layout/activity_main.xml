<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        android:id="@+id/app_bar_main"
        layout="@layout/app_bar_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/navigation_drawer_background"
        android:fitsSystemWindows="true"
        android:paddingTop="12dp"
        app:headerLayout="@layout/nav_header_main"
        app:itemIconTint="@color/nav_item_tint"
        app:itemRippleColor="@color/surface_chip_background"
        app:itemShapeAppearance="@style/ShapeAppearance.FeelOScope.NavigationItem"
        app:itemShapeInsetBottom="8dp"
        app:itemShapeInsetEnd="16dp"
        app:itemShapeInsetStart="16dp"
        app:itemShapeInsetTop="8dp"
        app:itemTextColor="@color/nav_item_tint"
        app:menu="@menu/activity_main_drawer" />
</androidx.drawerlayout.widget.DrawerLayout>
